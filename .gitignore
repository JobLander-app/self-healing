node_modules/
.env
*.log
# build output ignored for NEW code (watcher); dispatcher/dist is the recovered
# golden reference and stays TRACKED (gitignore does not affect tracked files)
dist/
# `node --test` compiles here (change-ingest, and now dispatcher)
dist-test/

# terraform (lock files ARE committed, like ai-voice-agent-python)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
.claude/

# Terraform-built function source archive (regenerated on every plan/apply).
infra/terraform/modules/self-healing-watchdog/.build/
