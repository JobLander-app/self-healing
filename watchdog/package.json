{
  "name": "self-healing-watchdog",
  "version": "1.0.0",
  "description": "Off-box watchdog for the self-healing VM: heartbeat staleness -> Telegram page + instance reset. Deterministic, no LLM.",
  "private": true,
  "main": "index.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0"
  }
}
