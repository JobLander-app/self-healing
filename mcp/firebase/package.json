{
  "name": "firebase-mcp-server",
  "version": "2.0.0",
  "description": "Vendored stdio MCP server for Firebase Firestore and Auth — read-capable investigation tools for the self-healing dispatcher (ADC-first auth, no key file on the VM)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "firebase-admin": "^12.0.0",
    "zod": "^3.23.8"
  }
}
